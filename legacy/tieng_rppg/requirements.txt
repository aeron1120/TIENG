# --- 데모 코어 (필수) ---
numpy>=1.23
scipy>=1.9
opencv-python>=4.7        # 실카메라 실행용. 헤드리스 서버는 opencv-python-headless
Pillow>=10.0               # 사이드 패널 UI 한글 라벨 렌더링(맑은고딕)

# --- 정량 검증(evaluate.py) 플롯 (선택) ---
matplotlib>=3.6

# --- 볼 다각형 ROI (--use-facemesh, 선택) ---
mediapipe>=0.10

# --- 암 진단 데모(cancer_demo.py, 선택) ---
scikit-learn>=1.3

# --- 로컬 대시보드(dashboard/app.py, 선택) ---
fastapi>=0.100
uvicorn>=0.22

# --- 열화상 MLX90640 실장비 연결(confidence/thermal_mlx90640.py, 선택) ---
# 라즈베리파이(Linux)에서만 설치할 것 — board/busio는 Adafruit Blinka로 GPIO/I2C가
# 있는 보드에서만 정상 동작한다. 하드웨어 없이 개발/selftest만 할 땐 필요 없음
# (selftest_thermal.py는 SyntheticThermalCamera만 쓴다).
# adafruit-circuitpython-mlx90640>=1.3
# adafruit-blinka>=8.0
