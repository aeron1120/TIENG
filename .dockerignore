# 이미지에 들어갈 이유가 없는 것들. 빌드 컨텍스트가 작아야 업로드도 캐시도 빠르다.
.git/
.venv/
__pycache__/
*.pyc
*.egg-info/

web/node_modules/
web/dist/

# 이식 전 프로토타입. 컨테이너에서 쓰지 않는다.
legacy/

tests/
scripts/
docs/
deploy/

# 런타임 산출물. 컨테이너 안에서 새로 만든다.
logs/*.csv
state/

.mypy_cache/
.ruff_cache/
.pytest_cache/
