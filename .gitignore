.venv/
__pycache__/
*.pyc

# 주행 기록. 한 세션에 수백 MB 다 (CLAUDE.md §8)
sessions/
# 리플레이 재처리 출력 (§11)
out/

# 프론트 빌드 산출물. api/openapi.json 과 web/fixtures/ 는 계약이라 커밋한다.
web/node_modules/
web/dist/

# 캘리브 결과는 기기마다 다르다 (§3)
config/calib.json

# 아카이브가 남긴 산출물. 코드가 아니라 실행 흔적이라 따라 들어올 이유가 없다.
legacy/touchfree_vitals/logs/*.csv
legacy/touchfree_vitals/web/dist/
legacy/touchfree_vitals/web/node_modules/
*.tsbuildinfo
legacy/tieng_rppg/confidence/calibration_*.json
legacy/tieng_rppg/confidence/coverage_mae_curve.png

# 옛 프로젝트 개요 PDF. 공개 저장소라 올리지 않는다. 파일은 로컬에 그대로 있다.
legacy/touchfree_vitals/TouchFree-Vitals-overview.pdf

# 옛 대시보드의 기기별 런타임 상태. 설정이 아니라 사용 흔적이다.
state/

.env

*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
